name: pre-commit

on:
  workflow_dispatch:  # Only run manually, disabled on push/PR

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    if: false  # Disabled - not needed for klyro
    steps:
      - run: echo "disabled"
