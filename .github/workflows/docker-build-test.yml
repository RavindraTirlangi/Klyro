name: Docker Build Test

on:
  workflow_dispatch:  # Only run manually

jobs:
  docker_build_and_push:
    runs-on: ubuntu-latest
    if: false  # Disabled - no Docker setup for klyro yet
    steps:
      - run: echo "disabled"
