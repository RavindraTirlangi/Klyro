name: Ubuntu Python Tests

on:
  workflow_dispatch:  # Only run manually

jobs:
  build:
    runs-on: ubuntu-latest
    if: false  # Disabled - enable when tests are ready for klyro
    steps:
      - run: echo "disabled"
